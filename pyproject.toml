[project]
name = "cardle"
version = "0.1.0"
description = "A car guessing game"
requires-python = ">=3.14"

dependencies = [
    "rdflib",
    "pyshacl",
    "requests",
    "neo4j",
]

[tool.setuptools.packages.find]
where = ["src"]